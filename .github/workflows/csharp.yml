name: csharp

on:
  push:
    paths:
      - csharp
    branches: [ master ]
  pull_request:
    paths:
      - csharp
    branches: [ master ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.301
    - name: Install dependencies
      run: dotnet restore csharp
    - name: Test
      run: dotnet test --no-restore --verbosity normal csharp
